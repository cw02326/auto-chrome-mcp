# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts
dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.onnx

# Environment variables
.env
.env.local
.env.*.local

# npm auth token (NEVER commit)
.npmrc

# Prevent npm metadata pollution
false/
metadata-v1.3/
registry.npmmirror.com/
registry.npmjs.com/

other/
tools_optimize.md
Agents.md
CLAUDE.md

**/*/coverage/*

.docs/
.claude/
# scalemaker fork — private RSA key for deterministic extension ID (NEVER commit)
app/chrome-extension/.keys/
.extension-id
.extension-pubkey-b64

# release 빌드 산출물 (gh release upload 로 별도 관리)
chrome-mcp-scalemaker-extension.zip
chrome-launcher-*.tar.gz

# local MCP server config (path is user-specific)
.mcp.json
