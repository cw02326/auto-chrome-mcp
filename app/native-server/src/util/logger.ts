// Reserved for future logger implementation.
// Previously contained commented-out debug logger pointing at hangwin's dev path.
// Removed in v1.0.29 cleanup. If you need file-backed logging, build on top of
// the report.ts log dir helpers (~/Library/Logs/, %LOCALAPPDATA%, etc.).
export {};
